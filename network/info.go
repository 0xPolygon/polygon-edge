package network

/*
// Info is the information of a peer
type Info struct {
	Client       string
	Enode        *enode.Enode
	Capabilities Capabilities
	ListenPort   uint64
}

// Capability is a feature of the peer
type Capability struct {
	Protocol protocol.Protocol
	Backend  protocol.Backend
}

// Capabilities is a list of capabilities of the peer
type Capabilities []*Capability

// NOTE: difference between advertise (capabilities) and the actual
// running protocols, those protocols should be registered in the peer
// likewise, the server (which is also a peer in itself) should have registered
// his running protocols
*/
